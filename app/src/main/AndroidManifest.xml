<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lq.comnui">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.EXPAND_STATUS_BAR" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".test.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".test.BarActivity"/>
        <activity android:name=".test.BarActivity1"/>
        <activity android:name=".test.DialogActivity"/>
        <activity android:name=".test.AppActivity"/>
        <activity android:name=".test.DeviceActivity"/>
        <activity android:name=".test.ScreenActivity"/>
        <activity android:name=".navigation.WeiboActivity"/>
        <activity android:name=".test.EditTextActivity"/>
        <activity android:name=".test.RecyclerViewActivity"/>
        <activity android:name=".test.SpliteActivity"/>
        
    </application>

</manifest>